# Freesurfer look up table (LUT)

A `data.frame` with the index, label, and RGBA (red, blue, green, alpha)
specification for the segmentations

## Usage

``` r
fs_lut
```

## Format

An object of class `data.frame` with 1266 rows and 6 columns.
