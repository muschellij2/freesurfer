% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tracker.R
\name{trac}
\alias{trac}
\alias{tracker}
\alias{trac_all}
\alias{trac_prep}
\alias{trac_bedpost}
\alias{trac_path}
\alias{tracker.help}
\title{FreeSurfer Diffusion Tractography Pipeline}
\usage{
tracker(
  infile = NULL,
  outdir = NULL,
  subjid = NULL,
  verbose = get_fs_verbosity(),
  opts = ""
)

trac_all(
  infile = NULL,
  outdir = NULL,
  subjid = NULL,
  verbose = get_fs_verbosity(),
  opts = ""
)

trac_prep(infile, outdir = NULL, subjid, verbose = get_fs_verbosity())

trac_bedpost(infile, outdir = NULL, subjid, verbose = get_fs_verbosity())

trac_path(infile, outdir = NULL, subjid, verbose = get_fs_verbosity())

tracker.help(display = TRUE, warn = TRUE, ...)
}
\arguments{
\item{infile}{Character; input DWI (diffusion-weighted imaging) filename
in DICOM or NIfTI format. Required for initial processing.}

\item{outdir}{Character, Path to output directory.}

\item{subjid}{subject id, if NULL, the basename of the infile will be used}

\item{verbose}{(logical) print diagnostic messages}

\item{opts}{Character. Additional options to Freesurfer function.}

\item{display}{Logical; whether to display help output}

\item{warn}{Logical; whether to warn if help is not available}

\item{...}{Additional arguments passed to \code{\link[=fs_help]{fs_help()}}}
}
\value{
Result of \code{\link[base:system]{base::system()}} call, typically exit status (0 = success).
}
\description{
These functions provide wrappers for FreeSurfer's \code{trac-all} command,
which performs automated reconstruction of diffusion pathways.
}
\details{
All functions call the same underlying FreeSurfer \code{trac-all} command with
different option flags.
}
\section{Functions}{
\itemize{
\item \code{tracker()}: Low-level wrapper for trac-all command with custom options

\item \code{trac_all()}: High-level wrapper running complete tractography pipeline

\item \code{trac_prep()}: Run pre-processing step (step 1: image corrections, registration)

\item \code{trac_bedpost()}: Run bedpost step (step 2: ball-and-stick model fitting)

\item \code{trac_path()}: Run pathway reconstruction step (step 3: probabilistic tractography)

\item \code{tracker.help()}: Display FreeSurfer help for trac-all

}}
\examples{
\dontshow{if (have_fs()) withAutoprint(\{ # examplesIf}
\dontrun{
# Run full tractography pipeline
trac_all(
  infile = "dwi.nii",
  subjid = "subject01",
  outdir = "~/tractography_output"
)

# Run step-by-step
trac_prep(infile = "dwi.nii", subjid = "subject02")
trac_bedpost(infile = "dwi.nii", subjid = "subject02")
trac_path(infile = "dwi.nii", subjid = "subject02")

# Run with custom options
tracker(
  infile = "dwi.nii",
  subjid = "subject03",
  opts = "-prep -bedp"
)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=recon_all]{recon_all()}} for structural reconstruction pipeline
}
