% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recon_manual.R
\name{recon_manual}
\alias{recon_manual}
\alias{recon_con1}
\alias{autorecon1}
\alias{recon_con2}
\alias{autorecon2}
\alias{recon_con3}
\alias{autorecon3}
\title{Manual Freesurfer Reconstruction Workflow}
\usage{
recon_con1(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())

autorecon1(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())

recon_con2(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())

autorecon2(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())

recon_con3(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())

autorecon3(infile, subjid, outdir = NULL, verbose = get_fs_verbosity())
}
\arguments{
\item{infile}{Input filename in DICOM (\code{.dcm}) or NIfTI (\code{.nii}) format.
This is the brain image file to be processed. If \code{NULL}, some \code{recon-all}
functionality may not work.}

\item{subjid}{subject id, if NULL, the basename of the infile will be used}

\item{outdir}{Output directory for storing reconstruction results.
If \code{NULL}, the default Freesurfer subject directory is used.}

\item{verbose}{(logical) print diagnostic messages}
}
\value{
The result of the \code{system} call to \code{recon-all}, either the command's
output or a diagnostic message if an issue occurs.
}
\description{
A set of helper functions for running different stages of Freesurfer's
\code{recon-all} pipeline: \code{autorecon1}, \code{autorecon2}, and \code{autorecon3}. These
functions are wrappers around the \link{reconner} function, simplifying the
reconstruction process by specifying pre-defined options for each stage.
}
\details{
Freesurfer is a widely used software suite for processing and analyzing
brain MRI images. The \code{recon-all} tool provides a stepwise reconstruction
process:
\itemize{
\item \code{-autorecon1}: Motion correction to skull stripping (steps 1-5)
\item \code{-autorecon2}: Further processing, including intensity normalization
\item \code{-autorecon3}: Final surface generation and QC measures
}

These helpers call specific stages of the \code{recon-all} pipeline to provide
finer control over processing.
}
\section{Functions}{
\itemize{
\item \code{recon_con1()}: Performs \code{-autorecon1}, which includes steps 1-5 of
Freesurfer's reconstruction process.

\item \code{autorecon1()}: Helper function for \code{recon_con1} with the same functionality.

\item \code{recon_con2()}: Performs \code{-autorecon2}, which includes steps 6+ of
Freesurfer's reconstruction process.

\item \code{autorecon2()}: Helper function for \code{recon_con2} with the same functionality.

\item \code{recon_con3()}: Performs the final stage of Freesurfer's
reconstruction process, which includes generating cortical surfaces,

\item \code{autorecon3()}: Helper function for \code{recon_con3} with the same functionality.

}}
\section{Note}{

See the official Freesurfer documentation for additional details on each
autorecon stage:
\url{https://surfer.nmr.mgh.harvard.edu/fswiki/recon-all}.

If \code{infile} is set to \code{NULL}, the \code{-i} flag (input file flag) in \code{recon-all}
will be omitted, which may or may not be appropriate depending on your use
case.
}

\examples{
\dontrun{
# Example: Perform Step 1-5 reconstruction (Motion Correction to Skull Strip)
recon_con1(
  infile = "subject_001.nii",
  outdir = "/output_dir",
  subjid = "subj01",
  verbose = TRUE
)

# Example: Run autorecon1 with equivalent helper function
autorecon1(
  infile = "subject_001.nii",
  outdir = "/output_dir",
  subjid = "subj01",
  verbose = TRUE
)

# Example: Perform further processing for Step 6+ using autorecon2
autorecon2(
  infile = "subject_002.nii",
  outdir = "/output_dir",
  subjid = "subj02",
  verbose = TRUE
)

# Example: Complete reconstruction with autorecon3
autorecon3(
  infile = "subject_003.nii",
  outdir = "/output_dir",
  subjid = "subj03",
  verbose = FALSE
)
}
}
\seealso{
\link{reconner}, \link{recon}, \link{recon_all}
}
