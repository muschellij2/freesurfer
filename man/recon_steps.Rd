% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recon_all.R
\name{recon_steps}
\alias{recon_steps}
\title{Reconstruction Steps for Neuroimaging Analysis}
\usage{
recon_steps()
}
\value{
A named logical vector, where each name represents a reconstruction
step (e.g., "motioncor", "talairach"), and the corresponding value
(\code{TRUE}) indicates that the step is included in the standard pipeline.
}
\description{
This function generates a named vector of logical flags
corresponding to the steps involved in a neuroimaging processing
pipeline. The function is designed to facilitate the control and
customization of individual steps in processes such as brain image
reconstruction (e.g., as used in the \href{https://surfer.nmr.mgh.harvard.edu/}{FreeSurfer}
software suite). Each step in the pipeline is associated with a
logical value (\code{TRUE}), indicating whether it is included as a default
part of the pipeline.
}
\details{
Below is a description of the primary steps represented in the
returned vector:
\itemize{
\item \strong{motioncor}: Motion correction of images.
\item \strong{nuintensitycor}: Non-uniform intensity correction.
\item \strong{talairach}: Transformation to Talairach space.
\item \strong{normalization}: Normalization of brain image intensity levels.
\item \strong{skullstrip}: Skull stripping for isolating the brain region.
\item \strong{gcareg & canorm}: General and cortical surface alignment and normalization.
\item \strong{rmneck}: Removal of the neck region from the dataset.
\item \strong{skull-lta}: Linear transformation for skull registration.
\item \strong{calabel}: Surface labeling with anatomical structures.
\item \strong{normalization2}: Secondary normalization step.
\item \strong{segmentation}: Segmentation of brain regions.
\item \strong{fill}: Surface filling for better reconstruction.
\item \strong{tessellate}: Creation of a tessellated mesh for surface modeling.
\item \strong{inflate1 & inflate2}: Inflation of surface meshes for curvature evaluation.
\item \strong{smooth1 & smooth2}: Smoothing of the surface meshes.
\item \strong{qsphere & sphere}: Generation of spherical surface representations.
\item \strong{surfreg & contrasurfreg}: Surface registration processes.
\item \strong{finalsurfs}: Construction of final brain surface models.
\item \strong{avgcurv}: Calculation of average cortical curvature.
\item \strong{cortparc & cortparc2}: Cortical parcellation into anatomical regions.
\item \strong{parcstats & parcstats2}: Summarization of parcellation statistics.
\item \strong{aparc2aseg}: Label projection from parcellations to a volume segmentation.
}

This logical vector can be directly used for toggling reconstruction
steps during neuroimaging workflows. Users may modify or customize
this output as needed for specific applications.
}
\examples{
# Get a vector of reconstruction steps
steps <- recon_steps()

# Check the default inclusion of the 'motioncor' step
steps["motioncor"]  # Output: TRUE

# Modify a specific reconstruction step (e.g., skip 'normalization')
steps["normalization"] <- FALSE

print(steps)

# Use the modified steps in a downstream neuroimaging script


}
